options:
  parameters:
    author: ''
    catch_exceptions: 'True'
    category: '[GRC Hier Blocks]'
    cmake_opt: ''
    comment: ''
    copyright: ''
    description: ''
    gen_cmake: 'On'
    gen_linking: dynamic
    generate_options: no_gui
    hier_block_src_path: '.:'
    id: alpha_stable_generator
    max_nouts: '0'
    output_language: python
    placement: (0,0)
    qt_qss_theme: ''
    realtime_scheduling: ''
    run: 'True'
    run_command: '{python} -u {filename}'
    run_options: run
    sizing_mode: fixed
    thread_safe_setters: ''
    title: Alpha stable simulation
    window_size: (1000,1000)
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 8]
    rotation: 0
    state: enabled

blocks:
- name: alpha_map
  id: variable
  parameters:
    comment: ''
    value: '[float(x) for x in alpha_map_str.split(",")]'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [584, 16.0]
    rotation: 0
    state: enabled
- name: beta_map
  id: variable
  parameters:
    comment: ''
    value: '[float(x) for x in beta_map_str.split(",")]'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [584, 128.0]
    rotation: 0
    state: enabled
- name: gama_map
  id: variable
  parameters:
    comment: ''
    value: '[float(x) for x in gama_map_str.split(",")]'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [584, 240.0]
    rotation: 0
    state: enabled
- name: samp_rate
  id: variable
  parameters:
    comment: ''
    value: '64000'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [256, 8.0]
    rotation: 0
    state: enabled
- name: L
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: intx
    value: '20'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1168, 116.0]
    rotation: 0
    state: enabled
- name: alpha_map_str
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: '"1.2,1.4,1.6,1.8"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 12.0]
    rotation: 0
    state: enabled
- name: analog_random_source_x_0
  id: analog_random_source_x
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    max: '255'
    maxoutbuf: '0'
    min: '0'
    minoutbuf: '0'
    num_samps: source_number_of_samples
    repeat: 'False'
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [16, 680.0]
    rotation: 0
    state: enabled
- name: analog_sig_source_x_0
  id: analog_sig_source_x
  parameters:
    affinity: ''
    alias: ''
    amp: '1'
    comment: ''
    freq: '1000'
    maxoutbuf: '0'
    minoutbuf: '0'
    offset: '0'
    phase: '0'
    samp_rate: samp_rate
    showports: 'False'
    type: complex
    waveform: analog.GR_COS_WAVE
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [784, 896.0]
    rotation: 0
    state: disabled
- name: beta_map_str
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: '"-1.0,1.0"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 124.0]
    rotation: 0
    state: enabled
- name: blocks_file_sink_0
  id: blocks_file_sink
  parameters:
    affinity: ''
    alias: ''
    append: 'False'
    comment: ''
    file: source_file
    type: byte
    unbuffered: 'False'
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [496, 860.0]
    rotation: 0
    state: enabled
- name: decoded_file
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: r"C:\Users\ANEDCD~1\Desktop\3.letnik\Diploma\simulations\decoded.bin"
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [896, 228.0]
    rotation: 0
    state: enabled
- name: encoded_file
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: r"C:\Users\ANEDCD~1\Desktop\3.letnik\Diploma\simulations\encoded.bin"
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [896, 116.0]
    rotation: 0
    state: enabled
- name: eos_timeout
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: EOS_timeout
    short_id: ''
    type: eng_float
    value: '3.0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1448, 144.0]
    rotation: 0
    state: enabled
- name: epy_block_0
  id: epy_block
  parameters:
    _source_code: "\"\"\"Shared framed alpha-stable SDR protocol used by sender and\
      \ receiver.\n\nThe synchronization word and header are deterministic BPSK. \
      \ Only the payload\nuses alpha-stable symbols, so acquisition does not depend\
      \ on a statistical\nestimate and the payload modulation remains unchanged in\
      \ principle.\n\"\"\"\n\nimport struct\nimport sys\nimport time\nimport zlib\n\
      \nimport numpy as np\nfrom gnuradio import gr\n\n\nSYNC_WORD = 0x1ACFFC1D  #\
      \ 32-bit CCSDS attached synchronization marker\nMIN_SYNC_SYMBOLS = 32\nMAGIC\
      \ = 0xD39A\nHEADER_BYTES = 10  # uint16 magic, uint32 payload length, uint32\
      \ CRC32\n\n\ndef bytes_to_bits(data):\n    \"\"\"Return least-significant-bit-first\
      \ bits, matching the payload mapping.\"\"\"\n    values = np.frombuffer(bytes(data),\
      \ dtype=np.uint8)\n    return np.unpackbits(values, bitorder=\"little\").astype(np.uint8,\
      \ copy=False)\n\n\ndef bits_to_bytes(bits):\n    bits = np.asarray(bits, dtype=np.uint8)\n\
      \    if len(bits) % 8:\n        raise ValueError(\"Number of bits must be a\
      \ multiple of 8\")\n    return np.packbits(bits, bitorder=\"little\").tobytes()\n\
      \n\ndef sync_pattern(symbols=MIN_SYNC_SYMBOLS):\n    \"\"\"Build a deterministic,\
      \ non-periodic +/-1 synchronization sequence.\"\"\"\n    symbols = max(MIN_SYNC_SYMBOLS,\
      \ int(symbols))\n    word_bits = np.asarray(\n        [(SYNC_WORD >> (31 - index))\
      \ & 1 for index in range(32)],\n        dtype=np.float32,\n    )\n    bits =\
      \ np.resize(word_bits, symbols)\n    return np.where(bits > 0, 1.0, -1.0).astype(np.float32)\n\
      \n\nclass alpha_encoder(gr.basic_block):\n    \"\"\"Frame bytes and encode payload\
      \ bits as skewed alpha-stable symbols.\"\"\"\n\n    def __init__(\n        self,\n\
      \        alpha_map=(1.2, 1.4, 1.6, 1.8),\n        beta_map=(-1.0, 1.0),\n  \
      \      gama_map=(0.5, 1.0, 1.5, 2.0),\n        samples_per_symbol=500,\n   \
      \     encode_alpha=False,\n        encode_beta=True,\n        encode_gama=False,\n\
      \        eos_timeout=2.0,\n        expected_input_bytes=None,\n        sync_symbols=MIN_SYNC_SYMBOLS,\n\
      \        header_repetitions=3,\n        tx_amplitude=0.70,\n        payload_scale=0.05,\n\
      \        sample_rate=64000,\n        subcarrier_frequency=4000,\n        payload_pilot_amplitude=0.70,\n\
      \        tx_tail_symbols=8,\n    ):\n        gr.basic_block.__init__(\n    \
      \        self, name=\"alpha_encoder\", in_sig=[np.uint8], out_sig=[np.complex64]\n\
      \        )\n\n        self.alpha_map = np.asarray(alpha_map, dtype=np.float64)\n\
      \        self.beta_map = np.asarray(beta_map, dtype=np.float64)\n        self.gama_map\
      \ = np.asarray(gama_map, dtype=np.float64)\n        self.samples_per_symbol\
      \ = int(samples_per_symbol)\n        self.encode_alpha = bool(encode_alpha)\n\
      \        self.encode_beta = bool(encode_beta)\n        self.encode_gama = bool(encode_gama)\n\
      \        self.eos_timeout = float(eos_timeout)\n        self.expected_input_bytes\
      \ = (\n            None if expected_input_bytes is None else int(expected_input_bytes)\n\
      \        )\n        self.sync_symbols = max(MIN_SYNC_SYMBOLS, int(sync_symbols))\n\
      \        self.header_repetitions = int(header_repetitions)\n        self.tx_amplitude\
      \ = float(tx_amplitude)\n        self.payload_scale = float(payload_scale)\n\
      \        self.sample_rate = float(sample_rate)\n        self.subcarrier_frequency\
      \ = float(subcarrier_frequency)\n        self.payload_pilot_amplitude = float(payload_pilot_amplitude)\n\
      \        self.tx_tail_symbols = int(tx_tail_symbols)\n\n        if self.samples_per_symbol\
      \ <= 0:\n            raise ValueError(\"samples_per_symbol must be positive\"\
      )\n        if len(self.beta_map) != 2 or not self.encode_beta:\n           \
      \ raise ValueError(\"Hardware protocol requires two beta values\")\n       \
      \ if self.encode_alpha or self.encode_gama:\n            raise ValueError(\"\
      Framed hardware protocol currently encodes beta only\")\n        if self.header_repetitions\
      \ < 1 or self.header_repetitions % 2 == 0:\n            raise ValueError(\"\
      header_repetitions must be a positive odd number\")\n        if not (0.0 < self.tx_amplitude\
      \ <= 0.95):\n            raise ValueError(\"tx_amplitude must be in (0, 0.95]\"\
      )\n        if self.payload_scale <= 0.0:\n            raise ValueError(\"payload_scale\
      \ must be positive\")\n        if self.sample_rate <= 0.0:\n            raise\
      \ ValueError(\"sample_rate must be positive\")\n        if abs(self.subcarrier_frequency)\
      \ >= self.sample_rate / 4.0:\n            raise ValueError(\"subcarrier_frequency\
      \ must be below sample_rate/4\")\n        if not (0.0 < self.payload_pilot_amplitude\
      \ <= 0.95):\n            raise ValueError(\"payload_pilot_amplitude must be\
      \ in (0, 0.95]\")\n        if self.tx_tail_symbols < 1:\n            raise ValueError(\"\
      tx_tail_symbols must be positive\")\n\n        self._input = bytearray()\n \
      \       self._output_fifo = np.empty(0, dtype=np.complex64)\n        self._frame_prepared\
      \ = False\n        self._ever_received_input = False\n        self._last_input_time\
      \ = None\n\n        print(\n            \"\\n[ENC INIT] protocol=PN32+REPEATED_HEADER+PILOTED_ALPHA_STABLE_PAYLOAD\"\
      ,\n            file=sys.stderr,\n            flush=True,\n        )\n      \
      \  print(\n            f\"[ENC INIT] samples_per_symbol={self.samples_per_symbol}\
      \ \"\n            f\"sync_symbols={self.sync_symbols} \"\n            f\"header_repetitions={self.header_repetitions}\"\
      ,\n            file=sys.stderr,\n            flush=True,\n        )\n      \
      \  print(\n            f\"[ENC INIT] tx_amplitude={self.tx_amplitude:.3f} \"\
      \n            f\"payload_scale={self.payload_scale:.4f} \"\n            f\"\
      subcarrier={self.subcarrier_frequency:.0f} Hz \"\n            f\"payload_pilot={self.payload_pilot_amplitude:.3f}\
      \ \"\n            f\"tx_tail_symbols={self.tx_tail_symbols}\",\n           \
      \ file=sys.stderr,\n            flush=True,\n        )\n\n    def forecast(self,\
      \ noutput_items, ninputs):\n        if self._frame_prepared or len(self._output_fifo):\n\
      \            return [0] * int(ninputs)\n        return [1] * int(ninputs)\n\n\
      \    @staticmethod\n    def alpha_stable_batch(beta_values, samples_per_symbol,\
      \ alpha=1.1, gamma=1.0):\n        beta_values = np.asarray(beta_values, dtype=np.float64)\n\
      \        count = len(beta_values) * int(samples_per_symbol)\n        if count\
      \ == 0:\n            return np.empty(0, dtype=np.float32)\n\n        beta =\
      \ np.repeat(beta_values, int(samples_per_symbol))\n        v = np.random.uniform(-np.pi\
      \ / 2.0, np.pi / 2.0, count)\n        w = np.maximum(np.random.exponential(1.0,\
      \ count), 1e-12)\n        constant = beta * np.tan(np.pi * alpha / 2.0)\n  \
      \      shift = np.arctan(constant)\n        scale = (1.0 + constant * constant)\
      \ ** (1.0 / (2.0 * alpha))\n        samples = (\n            scale\n       \
      \     * np.sin(alpha * v + shift)\n            / np.cos(v) ** (1.0 / alpha)\n\
      \            * (np.cos((1.0 - alpha) * v - shift) / w)\n            ** ((1.0\
      \ - alpha) / alpha)\n        )\n        samples = np.nan_to_num(samples * gamma,\
      \ nan=0.0, posinf=0.0, neginf=0.0)\n        return samples.astype(np.float32,\
      \ copy=False)\n\n    def _deterministic_symbols(self, values):\n        values\
      \ = np.asarray(values, dtype=np.float32) * self.tx_amplitude\n        return\
      \ np.repeat(values, self.samples_per_symbol).astype(np.complex64)\n\n    def\
      \ build_frame(self, payload):\n        \"\"\"Build one complete contiguous transmit\
      \ burst.\"\"\"\n        payload = bytes(payload)\n        crc = zlib.crc32(payload)\
      \ & 0xFFFFFFFF\n\n        preamble = self._deterministic_symbols(sync_pattern(self.sync_symbols))\n\
      \n        # The header is deterministic and repeated so that packet length and\n\
      \        # the diagnostic CRC can be recovered before decoding stochastic data.\n\
      \        header = struct.pack(\"<HII\", MAGIC, len(payload), crc)\n        header_bits\
      \ = bytes_to_bits(header)\n        repeated_header_bits = np.tile(header_bits,\
      \ self.header_repetitions)\n        header_samples = self._deterministic_symbols(\n\
      \            np.where(repeated_header_bits > 0, 1.0, -1.0)\n        )\n\n  \
      \      payload_bits = bytes_to_bits(payload)\n        beta_values = self.beta_map[payload_bits]\n\
      \        payload_real = self.alpha_stable_batch(\n            beta_values, self.samples_per_symbol,\
      \ alpha=1.1, gamma=1.0\n        )\n        # UHD expects normalized complex\
      \ samples.  Explicit limiting prevents\n        # undocumented DAC clipping\
      \ while retaining the skew sign in the tails.\n        payload_real = np.clip(\n\
      \            payload_real * self.payload_scale,\n            -self.tx_amplitude,\n\
      \            self.tx_amplitude,\n        )\n        alpha_symbols = payload_real.reshape(-1,\
      \ self.samples_per_symbol)\n        # Every information bit is still represented\
      \ only by the beta of its\n        # alpha-stable data symbol.  The preceding\
      \ deterministic symbol is a\n        # local phase/frequency reference and carries\
      \ no information.\n        pilot_symbols = np.full_like(\n            alpha_symbols,\
      \ self.payload_pilot_amplitude, dtype=np.float32\n        )\n        pairs =\
      \ np.empty(\n            (len(payload_bits), 2, self.samples_per_symbol), dtype=np.complex64\n\
      \        )\n        pairs[:, 0, :] = pilot_symbols\n        pairs[:, 1, :] =\
      \ alpha_symbols\n        payload_samples = pairs.reshape(-1)\n\n        # Keep\
      \ the final data symbols away from UHD's end-of-burst boundary.\n        # Without\
      \ a guard tail, the receiver can observe noise in place of the\n        # last\
      \ part of the payload when the B210 transmit stream shuts down.\n        # A\
      \ positive tone maintains a continuous envelope; it is not decoded.\n      \
      \  tail_samples = np.full(\n            self.tx_tail_symbols * self.samples_per_symbol,\n\
      \            np.complex64(self.payload_pilot_amplitude + 0j),\n            dtype=np.complex64,\n\
      \        )\n\n        frame = np.concatenate(\n            (preamble, header_samples,\
      \ payload_samples, tail_samples)\n        )\n        if self.subcarrier_frequency:\n\
      \            index = np.arange(len(frame), dtype=np.float64)\n            frame\
      \ = frame * np.exp(\n                1j * 2.0 * np.pi * self.subcarrier_frequency\
      \ * index / self.sample_rate\n            )\n            frame = frame.astype(np.complex64,\
      \ copy=False)\n        print(\n            f\"[ENC FRAME] payload={len(payload)}\
      \ bytes crc=0x{crc:08X} \"\n            f\"preamble={len(preamble)} header={len(header_samples)}\
      \ \"\n            f\"payload_samples={len(payload_samples)} tail={len(tail_samples)}\
      \ \"\n            f\"total={len(frame)}\",\n            file=sys.stderr,\n \
      \           flush=True,\n        )\n        return frame\n\n    def general_work(self,\
      \ input_items, output_items):\n        incoming = input_items[0]\n        out\
      \ = output_items[0]\n\n        if len(incoming):\n            self._ever_received_input\
      \ = True\n            self._last_input_time = time.monotonic()\n           \
      \ remaining = None\n            if self.expected_input_bytes is not None:\n\
      \                remaining = max(0, self.expected_input_bytes - len(self._input))\n\
      \            accepted = len(incoming) if remaining is None else min(len(incoming),\
      \ remaining)\n            if accepted:\n                self._input.extend(np.asarray(incoming[:accepted],\
      \ dtype=np.uint8).tobytes())\n            self.consume(0, len(incoming))\n\n\
      \        complete = (\n            self.expected_input_bytes is not None\n \
      \           and len(self._input) >= self.expected_input_bytes\n        )\n \
      \       timed_out = (\n            self.expected_input_bytes is None\n     \
      \       and self._ever_received_input\n            and self._last_input_time\
      \ is not None\n            and time.monotonic() - self._last_input_time >= self.eos_timeout\n\
      \        )\n\n        # Prepare the entire packet before emitting its first\
      \ sample.  This\n        # removes the preamble-to-payload underflow seen with\
      \ the USRP sink.\n        if not self._frame_prepared and (complete or timed_out):\n\
      \            self._output_fifo = self.build_frame(self._input)\n           \
      \ self._frame_prepared = True\n\n        if len(self._output_fifo):\n      \
      \      count = min(len(out), len(self._output_fifo))\n            out[:count]\
      \ = self._output_fifo[:count]\n            self._output_fifo = self._output_fifo[count:]\n\
      \            return count\n\n        if self._frame_prepared:\n            print(\"\
      [ENC] WORK_DONE\", file=sys.stderr, flush=True)\n            return gr.WORK_DONE\n\
      \        return 0"
    affinity: ''
    alias: ''
    alpha_map: alpha_map
    beta_map: beta_map
    comment: ''
    encode_alpha: 'False'
    encode_beta: 'True'
    encode_gama: 'False'
    eos_timeout: eos_timeout
    expected_input_bytes: source_number_of_samples
    gama_map: gama_map
    header_repetitions: '3'
    maxoutbuf: '0'
    minoutbuf: '0'
    payload_pilot_amplitude: '0.70'
    payload_scale: '0.05'
    sample_rate: samp_rate
    samples_per_symbol: samples_per_symbol
    subcarrier_frequency: '4000'
    sync_symbols: '32'
    tx_amplitude: '0.70'
    tx_tail_symbols: '8'
  states:
    _io_cache: ('alpha_encoder', 'alpha_encoder', [('alpha_map', '(1.2, 1.4, 1.6,
      1.8)'), ('beta_map', '(-1.0, 1.0)'), ('gama_map', '(0.5, 1.0, 1.5, 2.0)'), ('samples_per_symbol',
      '500'), ('encode_alpha', 'False'), ('encode_beta', 'True'), ('encode_gama',
      'False'), ('eos_timeout', '2.0'), ('expected_input_bytes', 'None'), ('sync_symbols',
      '32'), ('header_repetitions', '3'), ('tx_amplitude', '0.7'), ('payload_scale',
      '0.05'), ('sample_rate', '64000'), ('subcarrier_frequency', '4000'), ('payload_pilot_amplitude',
      '0.7'), ('tx_tail_symbols', '8')], [('0', 'byte', 1)], [('0', 'complex', 1)],
      'Frame bytes and encode payload bits as skewed alpha-stable symbols.', ['alpha_map',
      'beta_map', 'encode_alpha', 'encode_beta', 'encode_gama', 'eos_timeout', 'expected_input_bytes',
      'gama_map', 'header_repetitions', 'payload_pilot_amplitude', 'payload_scale',
      'sample_rate', 'samples_per_symbol', 'subcarrier_frequency', 'sync_symbols',
      'tx_amplitude', 'tx_tail_symbols'])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [712, 432.0]
    rotation: 0
    state: enabled
- name: gama_map_str
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: '"0.5,1.0,1.5,2.0"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 240.0]
    rotation: 0
    state: enabled
- name: noise_ratio
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: eng_float
    value: '10'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1408, 296.0]
    rotation: 0
    state: enabled
- name: samples_per_symbol
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: intx
    value: '500'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1168, 4.0]
    rotation: 0
    state: enabled
- name: source_file
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: r"source.bin"
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [896, 4.0]
    rotation: 0
    state: enabled
- name: source_number_of_samples
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: intx
    value: '128'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1424, 8.0]
    rotation: 0
    state: enabled
- name: uhd_usrp_sink_0
  id: uhd_usrp_sink
  parameters:
    affinity: ''
    alias: ''
    ant0: '"TX/RX"'
    ant1: '"TX/RX"'
    ant10: '"TX/RX"'
    ant11: '"TX/RX"'
    ant12: '"TX/RX"'
    ant13: '"TX/RX"'
    ant14: '"TX/RX"'
    ant15: '"TX/RX"'
    ant16: '"TX/RX"'
    ant17: '"TX/RX"'
    ant18: '"TX/RX"'
    ant19: '"TX/RX"'
    ant2: '"TX/RX"'
    ant20: '"TX/RX"'
    ant21: '"TX/RX"'
    ant22: '"TX/RX"'
    ant23: '"TX/RX"'
    ant24: '"TX/RX"'
    ant25: '"TX/RX"'
    ant26: '"TX/RX"'
    ant27: '"TX/RX"'
    ant28: '"TX/RX"'
    ant29: '"TX/RX"'
    ant3: '"TX/RX"'
    ant30: '"TX/RX"'
    ant31: '"TX/RX"'
    ant4: '"TX/RX"'
    ant5: '"TX/RX"'
    ant6: '"TX/RX"'
    ant7: '"TX/RX"'
    ant8: '"TX/RX"'
    ant9: '"TX/RX"'
    bw0: '200000'
    bw1: '0'
    bw10: '0'
    bw11: '0'
    bw12: '0'
    bw13: '0'
    bw14: '0'
    bw15: '0'
    bw16: '0'
    bw17: '0'
    bw18: '0'
    bw19: '0'
    bw2: '0'
    bw20: '0'
    bw21: '0'
    bw22: '0'
    bw23: '0'
    bw24: '0'
    bw25: '0'
    bw26: '0'
    bw27: '0'
    bw28: '0'
    bw29: '0'
    bw3: '0'
    bw30: '0'
    bw31: '0'
    bw4: '0'
    bw5: '0'
    bw6: '0'
    bw7: '0'
    bw8: '0'
    bw9: '0'
    center_freq0: '915000000'
    center_freq1: '0'
    center_freq10: '0'
    center_freq11: '0'
    center_freq12: '0'
    center_freq13: '0'
    center_freq14: '0'
    center_freq15: '0'
    center_freq16: '0'
    center_freq17: '0'
    center_freq18: '0'
    center_freq19: '0'
    center_freq2: '0'
    center_freq20: '0'
    center_freq21: '0'
    center_freq22: '0'
    center_freq23: '0'
    center_freq24: '0'
    center_freq25: '0'
    center_freq26: '0'
    center_freq27: '0'
    center_freq28: '0'
    center_freq29: '0'
    center_freq3: '0'
    center_freq30: '0'
    center_freq31: '0'
    center_freq4: '0'
    center_freq5: '0'
    center_freq6: '0'
    center_freq7: '0'
    center_freq8: '0'
    center_freq9: '0'
    clock_rate: 0e0
    clock_source0: ''
    clock_source1: ''
    clock_source2: ''
    clock_source3: ''
    clock_source4: ''
    clock_source5: ''
    clock_source6: ''
    clock_source7: ''
    comment: ''
    dev_addr: '"serial=30F4194"'
    dev_args: ''
    gain0: '35'
    gain1: '0'
    gain10: '0'
    gain11: '0'
    gain12: '0'
    gain13: '0'
    gain14: '0'
    gain15: '0'
    gain16: '0'
    gain17: '0'
    gain18: '0'
    gain19: '0'
    gain2: '0'
    gain20: '0'
    gain21: '0'
    gain22: '0'
    gain23: '0'
    gain24: '0'
    gain25: '0'
    gain26: '0'
    gain27: '0'
    gain28: '0'
    gain29: '0'
    gain3: '0'
    gain30: '0'
    gain31: '0'
    gain4: '0'
    gain5: '0'
    gain6: '0'
    gain7: '0'
    gain8: '0'
    gain9: '0'
    gain_type0: default
    gain_type1: default
    gain_type10: default
    gain_type11: default
    gain_type12: default
    gain_type13: default
    gain_type14: default
    gain_type15: default
    gain_type16: default
    gain_type17: default
    gain_type18: default
    gain_type19: default
    gain_type2: default
    gain_type20: default
    gain_type21: default
    gain_type22: default
    gain_type23: default
    gain_type24: default
    gain_type25: default
    gain_type26: default
    gain_type27: default
    gain_type28: default
    gain_type29: default
    gain_type3: default
    gain_type30: default
    gain_type31: default
    gain_type4: default
    gain_type5: default
    gain_type6: default
    gain_type7: default
    gain_type8: default
    gain_type9: default
    len_tag_name: '""'
    lo_export0: 'False'
    lo_export1: 'False'
    lo_export10: 'False'
    lo_export11: 'False'
    lo_export12: 'False'
    lo_export13: 'False'
    lo_export14: 'False'
    lo_export15: 'False'
    lo_export16: 'False'
    lo_export17: 'False'
    lo_export18: 'False'
    lo_export19: 'False'
    lo_export2: 'False'
    lo_export20: 'False'
    lo_export21: 'False'
    lo_export22: 'False'
    lo_export23: 'False'
    lo_export24: 'False'
    lo_export25: 'False'
    lo_export26: 'False'
    lo_export27: 'False'
    lo_export28: 'False'
    lo_export29: 'False'
    lo_export3: 'False'
    lo_export30: 'False'
    lo_export31: 'False'
    lo_export4: 'False'
    lo_export5: 'False'
    lo_export6: 'False'
    lo_export7: 'False'
    lo_export8: 'False'
    lo_export9: 'False'
    lo_source0: internal
    lo_source1: internal
    lo_source10: internal
    lo_source11: internal
    lo_source12: internal
    lo_source13: internal
    lo_source14: internal
    lo_source15: internal
    lo_source16: internal
    lo_source17: internal
    lo_source18: internal
    lo_source19: internal
    lo_source2: internal
    lo_source20: internal
    lo_source21: internal
    lo_source22: internal
    lo_source23: internal
    lo_source24: internal
    lo_source25: internal
    lo_source26: internal
    lo_source27: internal
    lo_source28: internal
    lo_source29: internal
    lo_source3: internal
    lo_source30: internal
    lo_source31: internal
    lo_source4: internal
    lo_source5: internal
    lo_source6: internal
    lo_source7: internal
    lo_source8: internal
    lo_source9: internal
    maxoutbuf: '0'
    minoutbuf: '0'
    nchan: '1'
    num_mboards: '1'
    otw: ''
    samp_rate: samp_rate
    sd_spec0: ''
    sd_spec1: ''
    sd_spec2: ''
    sd_spec3: ''
    sd_spec4: ''
    sd_spec5: ''
    sd_spec6: ''
    sd_spec7: ''
    show_lo_controls: 'False'
    start_time: '-1.0'
    stream_args: ''
    stream_chans: '[]'
    sync: sync
    time_source0: ''
    time_source1: ''
    time_source2: ''
    time_source3: ''
    time_source4: ''
    time_source5: ''
    time_source6: ''
    time_source7: ''
    type: fc32
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1096, 504.0]
    rotation: 0
    state: enabled

connections:
- [analog_random_source_x_0, '0', blocks_file_sink_0, '0']
- [analog_random_source_x_0, '0', epy_block_0, '0']
- [epy_block_0, '0', uhd_usrp_sink_0, '0']

metadata:
  file_format: 1
  grc_version: 3.10.12.0
