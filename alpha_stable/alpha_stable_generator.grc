options:
  parameters:
    author: ''
    catch_exceptions: 'True'
    category: '[GRC Hier Blocks]'
    cmake_opt: ''
    comment: ''
    copyright: ''
    description: ''
    gen_cmake: 'On'
    gen_linking: dynamic
    generate_options: qt_gui
    hier_block_src_path: '.:'
    id: alpha_stable_generator
    max_nouts: '0'
    output_language: python
    placement: (0,0)
    qt_qss_theme: ''
    realtime_scheduling: ''
    run: 'True'
    run_command: '{python} -u {filename}'
    run_options: run
    sizing_mode: fixed
    thread_safe_setters: ''
    title: Alpha stable simulation
    window_size: (1000,1000)
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 8]
    rotation: 0
    state: enabled

blocks:
- name: alpha_map
  id: variable
  parameters:
    comment: ''
    value: '[float(x) for x in alpha_map_str.split(",")]'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [584, 16.0]
    rotation: 0
    state: enabled
- name: beta_map
  id: variable
  parameters:
    comment: ''
    value: '[float(x) for x in beta_map_str.split(",")]'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [584, 128.0]
    rotation: 0
    state: enabled
- name: gama_map
  id: variable
  parameters:
    comment: ''
    value: '[float(x) for x in gama_map_str.split(",")]'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [584, 240.0]
    rotation: 0
    state: enabled
- name: samp_rate
  id: variable
  parameters:
    comment: ''
    value: '32000'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [256, 8.0]
    rotation: 0
    state: enabled
- name: L
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: intx
    value: '4'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1168, 116.0]
    rotation: 0
    state: enabled
- name: alpha_map_str
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: '"1.2,1.4,1.6,1.8"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 12.0]
    rotation: 0
    state: enabled
- name: analog_noise_source_x_0
  id: analog_noise_source_x
  parameters:
    affinity: ''
    alias: ''
    amp: '0.2'
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    noise_type: analog.GR_GAUSSIAN
    seed: '0'
    type: float
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [400, 828.0]
    rotation: 0
    state: enabled
- name: analog_random_source_x_0
  id: analog_random_source_x
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    max: '255'
    maxoutbuf: '0'
    min: '0'
    minoutbuf: '0'
    num_samps: source_number_of_samples
    repeat: 'False'
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [16, 680.0]
    rotation: 0
    state: enabled
- name: beta_map_str
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: '"-1.0,1.0"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 124.0]
    rotation: 0
    state: enabled
- name: blocks_add_xx_0
  id: blocks_add_xx
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    num_inputs: '2'
    type: float
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [792, 572.0]
    rotation: 0
    state: enabled
- name: blocks_file_sink_0
  id: blocks_file_sink
  parameters:
    affinity: ''
    alias: ''
    append: 'False'
    comment: ''
    file: source_file
    type: byte
    unbuffered: 'False'
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [384, 972.0]
    rotation: 0
    state: enabled
- name: blocks_file_sink_1
  id: blocks_file_sink
  parameters:
    affinity: ''
    alias: ''
    append: 'False'
    comment: ''
    file: decoded_file
    type: byte
    unbuffered: 'False'
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1560, 556.0]
    rotation: 0
    state: enabled
- name: blocks_file_sink_2
  id: blocks_file_sink
  parameters:
    affinity: ''
    alias: ''
    append: 'False'
    comment: ''
    file: encoded_file
    type: float
    unbuffered: 'False'
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [784, 356.0]
    rotation: 0
    state: enabled
- name: blocks_head_0
  id: blocks_head
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    num_items: source_number_of_samples
    type: byte
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [240, 716.0]
    rotation: 0
    state: enabled
- name: blocks_head_1
  id: blocks_head
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    num_items: source_number_of_samples*8*samples_per_symbol
    type: float
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [536, 700.0]
    rotation: 0
    state: enabled
- name: blocks_head_2
  id: blocks_head
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    num_items: source_number_of_samples
    type: byte
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1288, 564.0]
    rotation: 0
    state: enabled
- name: blocks_throttle2_0
  id: blocks_throttle2
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    ignoretag: 'True'
    limit: auto
    maximum: '0.1'
    maxoutbuf: '0'
    minoutbuf: '0'
    samples_per_second: samp_rate
    type: byte
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [272, 544.0]
    rotation: 0
    state: enabled
- name: decoded_file
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: r"C:\Users\ANEDCD~1\Desktop\3.letnik\Diploma\simulations\decoded.bin"
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [896, 228.0]
    rotation: 0
    state: enabled
- name: encoded_file
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: r"C:\Users\ANEDCD~1\Desktop\3.letnik\Diploma\simulations\encoded.bin"
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [896, 116.0]
    rotation: 0
    state: enabled
- name: epy_block_0
  id: epy_block
  parameters:
    _source_code: "\"\"\"\nEmbedded Python Blocks:\n\nEach time this file is saved,\
      \ GRC will instantiate the first class it finds\nto get ports and parameters\
      \ of your block. The arguments to __init__ will\nbe the parameters. All of them\
      \ are required to have default values!\n\"\"\"\n\nimport numpy as np\nfrom gnuradio\
      \ import gr\n\n\nclass alpha_encoder(gr.basic_block):\n    \"\"\"Encode bytes\
      \ into alpha-stable sample bursts.\"\"\"\n\n    def __init__(self, alpha_map=[1.2,\
      \ 1.4, 1.6, 1.8], beta_map=[-1.0, -0.3, 0.3, 1.0], gama_map=[0.5, 1.0, 1.5,\
      \ 2.0], samples_per_symbol=500, encode_alpha=False, encode_beta=True, encode_gama=False):\n\
      \        gr.basic_block.__init__(\n            self,\n            name='alpha_encoder',\n\
      \            in_sig=[np.uint8],\n            out_sig=[np.float32],\n       \
      \ )\n        \n        self.alpha_map = list(alpha_map)\n        self.beta_map\
      \ = list(beta_map)\n        self.gama_map = list(gama_map)\n        self.samples_per_symbol\
      \ = int(samples_per_symbol)\n        self.encode_alpha = bool(encode_alpha)\n\
      \        self.encode_beta = bool(encode_beta)\n        self.encode_gama = bool(encode_gama)\n\
      \        self._bit_buffer = []\n\n        if self.samples_per_symbol <= 0:\n\
      \            raise ValueError(\"samples_per_symbol must be positive\")\n\n \
      \       if self.encode_alpha and not self.is_power_of_two(len(self.alpha_map)):\n\
      \            raise ValueError(\"alpha_map size must be power of 2\")\n     \
      \   if self.encode_beta and not self.is_power_of_two(len(self.beta_map)):\n\
      \            raise ValueError(\"beta_map size must be power of 2\")\n      \
      \  if self.encode_gama and not self.is_power_of_two(len(self.gama_map)):\n \
      \           raise ValueError(\"gama_map size must be power of 2\")\n\n     \
      \   self.bits_alpha = self.log2_za_potence2(len(self.alpha_map)) if self.encode_alpha\
      \ else 0\n        self.bits_beta = self.log2_za_potence2(len(self.beta_map))\
      \ if self.encode_beta else 0\n        self.bits_gama = self.log2_za_potence2(len(self.gama_map))\
      \ if self.encode_gama else 0\n        self.bits_per_symbol = self.bits_alpha\
      \ + self.bits_beta + self.bits_gama\n\n        if self.bits_per_symbol <= 0:\n\
      \            raise ValueError(\"At least one of encode_alpha, encode_beta, or\
      \ encode_gama must be enabled\")\n\n    def forecast(self, noutput_items, ninputs):\n\
      \        \"\"\"Non-blocking forecast lets encoder flush internal bit buffer\
      \ at EOS.\"\"\"\n        return [0] * int(ninputs)\n\n    @staticmethod\n  \
      \  def is_power_of_two(x):\n        return x > 0 and (x & (x - 1)) == 0\n\n\
      \    @staticmethod\n    def log2_za_potence2(x):\n        if x == 0:\n     \
      \       return 0\n\n        if alpha_encoder.is_power_of_two(x):\n         \
      \   vrednost = 0\n            while x > 1:\n                x >>= 1\n      \
      \          vrednost += 1\n            return vrednost\n        raise ValueError(\"\
      One of the maps is not of power 2^n\")\n\n    @staticmethod\n    def alpha_stable(size,\
      \ alpha=1.1, beta=0.0, gama=1.0):\n        v_values = np.random.uniform(-np.pi\
      \ / 2, np.pi / 2, size)\n        w_values = np.random.exponential(1, size)\n\
      \n        constant = beta * np.tan(np.pi * alpha / 2)\n        shift = np.arctan(constant)\n\
      \        scale = (1 + constant ** 2) ** (1 / (2 * alpha))\n\n        samples\
      \ = scale * np.sin(alpha * v_values + shift) / (np.cos(v_values)) ** (1 / alpha)\
      \ * (np.cos((1 - alpha) * v_values - shift) / w_values) ** ((1 - alpha) / alpha)\n\
      \        return gama * samples\n\n    @staticmethod\n    def _read_bits(bit_buffer,\
      \ start, count):\n        value = 0\n        for index in range(count):\n  \
      \          value |= (bit_buffer[start + index] << index)\n        return value\n\
      \n    def _append_input_bits(self, input_bytes):\n        for byte in input_bytes:\n\
      \            byte_value = int(byte)\n            for bit_index in range(8):\n\
      \                self._bit_buffer.append((byte_value >> bit_index) & 1)\n\n\
      \    def _encode_one_symbol(self, bit_offset):\n        alpha = 1.1\n      \
      \  beta = 0.0\n        gama = 1.0\n        offset = bit_offset\n\n        if\
      \ self.encode_alpha:\n            alpha_index = self._read_bits(self._bit_buffer,\
      \ offset, self.bits_alpha)\n            alpha = self.alpha_map[alpha_index]\n\
      \            offset += self.bits_alpha\n\n        if self.encode_beta:\n   \
      \         beta_index = self._read_bits(self._bit_buffer, offset, self.bits_beta)\n\
      \            beta = self.beta_map[beta_index]\n            offset += self.bits_beta\n\
      \n        if self.encode_gama:\n            gama_index = self._read_bits(self._bit_buffer,\
      \ offset, self.bits_gama)\n            gama = self.gama_map[gama_index]\n  \
      \          offset += self.bits_gama\n\n        return alpha, beta, gama\n\n\
      \    def general_work(self, input_items, output_items):\n        input_bytes\
      \ = input_items[0]\n        output_samples = output_items[0]\n\n        if len(input_bytes):\n\
      \            self._append_input_bits(input_bytes)\n\n        max_symbols = len(output_samples)\
      \ // self.samples_per_symbol\n        produced_symbols = 0\n        bit_offset\
      \ = 0\n\n        while produced_symbols < max_symbols and len(self._bit_buffer)\
      \ - bit_offset >= self.bits_per_symbol:\n            alpha, beta, gama = self._encode_one_symbol(bit_offset)\n\
      \            samples = self.alpha_stable(self.samples_per_symbol, alpha=alpha,\
      \ beta=beta, gama=gama).astype(np.float32)\n\n            start = produced_symbols\
      \ * self.samples_per_symbol\n            end = start + self.samples_per_symbol\n\
      \            output_samples[start:end] = samples\n\n            produced_symbols\
      \ += 1\n            bit_offset += self.bits_per_symbol\n\n        if bit_offset:\n\
      \            self._bit_buffer = self._bit_buffer[bit_offset:]\n\n        if\
      \ len(input_bytes):\n            self.consume(0, len(input_bytes))\n\n     \
      \   return produced_symbols * self.samples_per_symbol\n"
    affinity: ''
    alias: ''
    alpha_map: alpha_map
    beta_map: beta_map
    comment: ''
    encode_alpha: 'False'
    encode_beta: 'True'
    encode_gama: 'False'
    gama_map: gama_map
    maxoutbuf: '0'
    minoutbuf: '0'
    samples_per_symbol: samples_per_symbol
  states:
    _io_cache: ('alpha_encoder', 'alpha_encoder', [('alpha_map', '[1.2, 1.4, 1.6,
      1.8]'), ('beta_map', '[-1.0, -0.3, 0.3, 1.0]'), ('gama_map', '[0.5, 1.0, 1.5,
      2.0]'), ('samples_per_symbol', '500'), ('encode_alpha', 'False'), ('encode_beta',
      'True'), ('encode_gama', 'False')], [('0', 'byte', 1)], [('0', 'float', 1)],
      'Encode bytes into alpha-stable sample bursts.', ['alpha_map', 'beta_map', 'encode_alpha',
      'encode_beta', 'encode_gama', 'gama_map', 'samples_per_symbol'])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [480, 432.0]
    rotation: 0
    state: enabled
- name: epy_block_1
  id: epy_block
  parameters:
    L: L
    _source_code: "\"\"\"\nEmbedded Python Blocks:\n\nEach time this file is saved,\
      \ GRC will instantiate the first class it finds\nto get ports and parameters\
      \ of your block. The arguments to __init__ will\nbe the parameters. All of them\
      \ are required to have default values!\n\"\"\"\n\nimport numpy as np\nfrom gnuradio\
      \ import gr\n\n\nclass alpha_decoder(gr.basic_block):\n    \"\"\"Decode alpha-stable\
      \ sample bursts into a bit stream.\"\"\"\n\n    def __init__(self, alpha_map=[1.2,\
      \ 1.4, 1.6, 1.8], beta_map=[-1.0, -0.3, 0.3, 1.0], gama_map=[0.5, 1.0, 1.5,\
      \ 2.0], samples_per_symbol=500, L=10, encode_alpha=False, encode_beta=True,\
      \ encode_gama=False):\n        gr.basic_block.__init__(\n            self,\n\
      \            name='alpha_decoder',   # will show up in GRC\n            in_sig=[np.float32],\n\
      \            out_sig=[np.uint8],\n        )\n\n        self.alpha_map = list(alpha_map)\n\
      \        self.beta_map = list(beta_map)\n        self.gama_map = list(gama_map)\n\
      \        self.samples_per_symbol = int(samples_per_symbol)\n        self.L =\
      \ int(L)\n        self.encode_alpha = bool(encode_alpha)\n        self.encode_beta\
      \ = bool(encode_beta)\n        self.encode_gama = bool(encode_gama)\n      \
      \  self._sample_buffer = np.empty(0, dtype=np.float32)\n        self._decoded_bit_buffer\
      \ = []\n\n        if self.samples_per_symbol <= 0:\n            raise ValueError(\"\
      samples_per_symbol must be positive\")\n        if self.L <= 0:\n          \
      \  raise ValueError(\"L must be positive\")\n        if self.samples_per_symbol\
      \ % self.L != 0:\n            raise ValueError(\"samples_per_symbol must be\
      \ divisible by L\")\n        if self.encode_alpha and not self.is_power_of_two(len(self.alpha_map)):\n\
      \            raise ValueError(\"alpha_map size must be power of 2\")\n     \
      \   if self.encode_beta and not self.is_power_of_two(len(self.beta_map)):\n\
      \            raise ValueError(\"beta_map size must be power of 2\")\n      \
      \  if self.encode_gama and not self.is_power_of_two(len(self.gama_map)):\n \
      \           raise ValueError(\"gama_map size must be power of 2\")\n\n     \
      \   self.bits_alpha = self.log2_za_potence2(len(self.alpha_map)) if self.encode_alpha\
      \ else 0\n        self.bits_beta = self.log2_za_potence2(len(self.beta_map))\
      \ if self.encode_beta else 0\n        self.bits_gama = self.log2_za_potence2(len(self.gama_map))\
      \ if self.encode_gama else 0\n        self.bits_per_symbol = self.bits_alpha\
      \ + self.bits_beta + self.bits_gama\n\n        if self.bits_per_symbol <= 0:\n\
      \            raise ValueError(\"At least one of encode_alpha, encode_beta, or\
      \ encode_gama must be enabled\")\n\n        # This decoder currently implements\
      \ only beta estimation.\n        if self.encode_alpha or self.encode_gama:\n\
      \            raise ValueError(\"Decoder currently supports only encode_beta=True,\
      \ encode_alpha=False, encode_gama=False\")\n\n        self.expected_chunk =\
      \ self.samples_per_symbol\n\n    def forecast(self, noutput_items, ninputs):\n\
      \        \"\"\"Non-blocking forecast prevents starvation on finite input streams.\"\
      \"\"\n        return [0] * int(ninputs)\n\n    @staticmethod\n    def is_power_of_two(x):\n\
      \        return x > 0 and (x & (x - 1)) == 0\n\n    @staticmethod\n    def log2_za_potence2(x):\n\
      \        if x == 0:\n            return 0\n\n        if alpha_decoder.is_power_of_two(x):\n\
      \            vrednost = 0\n            while x > 1:\n                x >>= 1\n\
      \                vrednost += 1\n            return vrednost\n        raise ValueError(\"\
      One of the maps is not of power 2^n\")\n\n    @staticmethod\n    def closest_index(value,\
      \ values):\n        arr = np.array(values, dtype=float)\n        if arr.size\
      \ == 1:\n            return 0\n        return int(np.argmin(np.abs(arr - value)))\n\
      \n    def estimate_beta(self, symbol_samples):\n        \"\"\"Vectorized beta\
      \ estimator from paper.\"\"\"\n        samples_per_realization = self.samples_per_symbol\
      \ // self.L\n        if samples_per_realization <= 0:\n            return 0.0\n\
      \n        # Reshape into L segments and compute max/min per segment\n      \
      \  symbol_array = np.asarray(symbol_samples, dtype=np.float32)\n        segments\
      \ = symbol_array[:samples_per_realization * self.L].reshape(self.L, samples_per_realization)\n\
      \        \n        y_max = np.max(segments, axis=1)\n        y_min = np.min(segments,\
      \ axis=1)\n\n        if y_max.size == 0 or y_min.size == 0:\n            return\
      \ 0.0\n\n        s2_max = float(np.var(y_max, ddof=1)) if len(y_max) > 1 else\
      \ 0.0\n        s2_min = float(np.var(y_min, ddof=1)) if len(y_min) > 1 else\
      \ 0.0\n\n        s2_max = float(np.nan_to_num(s2_max, nan=0.0, posinf=0.0, neginf=0.0))\n\
      \        s2_min = float(np.nan_to_num(s2_min, nan=0.0, posinf=0.0, neginf=0.0))\n\
      \n        spread = np.sqrt(max(0.0, s2_max)) + np.sqrt(max(0.0, s2_min)) + 1e-12\n\
      \        score = (np.sqrt(max(0.0, s2_max)) - np.sqrt(max(0.0, s2_min))) / spread\n\
      \        return float(np.clip(score, -1.0, 1.0))\n\n    def _append_input_samples(self,\
      \ input_samples):\n        if input_samples.size:\n            self._sample_buffer\
      \ = np.concatenate((self._sample_buffer, input_samples.astype(np.float32, copy=False)))\n\
      \n    def general_work(self, input_items, output_items):\n        in_samples\
      \ = input_items[0]\n        out = output_items[0]\n\n        if len(out) ==\
      \ 0:\n            return 0\n\n        if len(in_samples) > 0:\n            self._append_input_samples(in_samples)\n\
      \            self.consume(0, len(in_samples))\n\n        out_index = 0\n\n \
      \       # Drain already decoded bytes first. This is important near end-of-stream.\n\
      \        buffered_bytes = len(self._decoded_bit_buffer) // 8\n        bytes_from_buffer\
      \ = min(len(out), buffered_bytes)\n        for byte_index in range(bytes_from_buffer):\n\
      \            base = byte_index * 8\n            byte_value = 0\n           \
      \ for bit_index in range(8):\n                byte_value |= (self._decoded_bit_buffer[base\
      \ + bit_index] & 0x1) << bit_index\n            out[out_index] = byte_value\n\
      \            out_index += 1\n\n        if bytes_from_buffer > 0:\n         \
      \   self._decoded_bit_buffer = self._decoded_bit_buffer[bytes_from_buffer *\
      \ 8:]\n\n        if out_index == len(out):\n            return out_index\n\n\
      \        available_symbols = len(self._sample_buffer) // self.expected_chunk\n\
      \        symbols_to_process = available_symbols\n\n        beta_mask = (1 <<\
      \ self.bits_beta) - 1 if self.bits_beta > 0 else 0\n\n        # First decode\
      \ all available symbols into a bit buffer.\n        for symbol_index in range(symbols_to_process):\n\
      \            symbol_start = symbol_index * self.expected_chunk\n           \
      \ symbol_end = symbol_start + self.expected_chunk\n            symbol_samples\
      \ = self._sample_buffer[symbol_start:symbol_end]\n\n            probably_beta\
      \ = self.estimate_beta(symbol_samples)\n            beta_idx = self.closest_index(probably_beta,\
      \ self.beta_map) & beta_mask if self.bits_beta > 0 else 0\n\n            # Emit\
      \ decoded field bits in encoder order: alpha, beta, gama.\n            for bit_index\
      \ in range(self.bits_alpha):\n                self._decoded_bit_buffer.append(0)\n\
      \            for bit_index in range(self.bits_beta):\n                self._decoded_bit_buffer.append((beta_idx\
      \ >> bit_index) & 0x1)\n            for bit_index in range(self.bits_gama):\n\
      \                self._decoded_bit_buffer.append(0)\n\n        consumed_samples\
      \ = symbols_to_process * self.expected_chunk\n        self._sample_buffer =\
      \ self._sample_buffer[consumed_samples:]\n\n        # Then pack every 8 decoded\
      \ bits back into output bytes.\n        available_bytes = len(self._decoded_bit_buffer)\
      \ // 8\n        bytes_to_output = min(len(out) - out_index, available_bytes)\n\
      \n        for byte_index in range(bytes_to_output):\n            base = byte_index\
      \ * 8\n            byte_value = 0\n            for bit_index in range(8):\n\
      \                byte_value |= (self._decoded_bit_buffer[base + bit_index] &\
      \ 0x1) << bit_index\n            out[out_index] = byte_value\n            out_index\
      \ += 1\n\n        if bytes_to_output > 0:\n            self._decoded_bit_buffer\
      \ = self._decoded_bit_buffer[bytes_to_output * 8:]\n\n        return out_index\n"
    affinity: ''
    alias: ''
    alpha_map: alpha_map
    beta_map: beta_map
    comment: ''
    encode_alpha: 'False'
    encode_beta: 'True'
    encode_gama: 'False'
    gama_map: gama_map
    maxoutbuf: '0'
    minoutbuf: '0'
    samples_per_symbol: samples_per_symbol
  states:
    _io_cache: ('alpha_decoder', 'alpha_decoder', [('alpha_map', '[1.2, 1.4, 1.6,
      1.8]'), ('beta_map', '[-1.0, -0.3, 0.3, 1.0]'), ('gama_map', '[0.5, 1.0, 1.5,
      2.0]'), ('samples_per_symbol', '500'), ('L', '10'), ('encode_alpha', 'False'),
      ('encode_beta', 'True'), ('encode_gama', 'False')], [('0', 'float', 1)], [('0',
      'byte', 1)], 'Decode alpha-stable sample bursts into a bit stream.', ['L', 'alpha_map',
      'beta_map', 'encode_alpha', 'encode_beta', 'encode_gama', 'gama_map', 'samples_per_symbol'])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [952, 492.0]
    rotation: 0
    state: enabled
- name: gama_map_str
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: '"0.5,1.0,1.5,2.0"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 240.0]
    rotation: 0
    state: enabled
- name: qtgui_sink_x_0
  id: qtgui_sink_x
  parameters:
    affinity: ''
    alias: ''
    bw: samp_rate
    comment: ''
    fc: '0'
    fftsize: '1024'
    gui_hint: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    name: '"Encoded wave"'
    plotconst: 'True'
    plotfreq: 'True'
    plottime: 'True'
    plotwaterfall: 'True'
    rate: '10'
    showports: 'False'
    showrf: 'False'
    type: float
    wintype: window.WIN_BLACKMAN_hARRIS
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [952, 764.0]
    rotation: 0
    state: enabled
- name: samples_per_symbol
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: intx
    value: '24'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1168, 4.0]
    rotation: 0
    state: enabled
- name: source_file
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: str
    value: r"C:\Users\ANEDCD~1\Desktop\3.letnik\Diploma\simulations\source.bin"
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [896, 4.0]
    rotation: 0
    state: enabled
- name: source_number_of_samples
  id: parameter
  parameters:
    alias: ''
    comment: ''
    hide: none
    label: ''
    short_id: ''
    type: intx
    value: '128'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1424, 8.0]
    rotation: 0
    state: enabled

connections:
- [analog_noise_source_x_0, '0', blocks_head_1, '0']
- [analog_random_source_x_0, '0', blocks_head_0, '0']
- [blocks_add_xx_0, '0', epy_block_1, '0']
- [blocks_head_0, '0', blocks_file_sink_0, '0']
- [blocks_head_0, '0', blocks_throttle2_0, '0']
- [blocks_head_1, '0', blocks_add_xx_0, '1']
- [blocks_head_2, '0', blocks_file_sink_1, '0']
- [blocks_throttle2_0, '0', epy_block_0, '0']
- [epy_block_0, '0', blocks_add_xx_0, '0']
- [epy_block_0, '0', blocks_file_sink_2, '0']
- [epy_block_0, '0', qtgui_sink_x_0, '0']
- [epy_block_1, '0', blocks_head_2, '0']

metadata:
  file_format: 1
  grc_version: 3.10.12.0
